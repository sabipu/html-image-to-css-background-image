# html-image-to-css-background-image
🙃 Npm package - Convert HTML image to CSS background image.

## Usage

```js
var cssBackgroundImage = new CSSBackgroundImage({
  className: '.bg-image',
  imageStyle: 'cover'
});
```